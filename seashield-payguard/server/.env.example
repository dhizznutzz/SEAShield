# Enable the AI reasoning layer by providing a Vercel AI Gateway key.
# Without this, SeaShield still works using heuristics + reputation only.
AI_GATEWAY_API_KEY=

# Optional: override the model routed through the gateway (provider/model string).
# SEASHIELD_MODEL=anthropic/claude-sonnet-5
